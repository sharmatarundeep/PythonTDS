# Case 3 - Run the remote script via remote server but from Local Machine like our laptop

# From the Mac OS terminal do the following 
# ssh tarundeep@st101 "sudo python3 /home/tarundeep/sniffer.py"
# This means open a SSH connection and run the specified command which is in ""..